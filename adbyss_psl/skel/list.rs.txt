#[derive(Clone, Copy, Eq, Hash, PartialEq)]
/// # Suffix List.
pub(super) enum Suffix {{
{suffixes}
}}

impl Suffix {{
	/// # From Slice.
{suffix_from_slice}

	/// # Is Wild?
	pub(super) const fn is_wild(self) -> bool {{
		matches!(self, {suffixes_wild})
	}}

	/// # Wild Match.
	pub(super) fn wild(self, src: &[u8]) -> bool {{
		match self {{
{suffix_wild_arms}
		}}
	}}

{suffix_from_slice_len}
}}
